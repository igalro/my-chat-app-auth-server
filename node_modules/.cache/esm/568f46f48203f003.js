let winston,config;_820‍.x([["default",()=>_820‍.o]]);_820‍.w("winston",[["default",["winston"],function(v){winston=v}]]);_820‍.w("config",[["default",["config"],function(v){config=v}]]);

const {env} = config;
const { level } = config.logger;

const serviceName = "frontend-server";

const logger = winston.createLogger({
    level,
    format: winston.format.combine(
        winston.format.timestamp(),
        winston.format.json(),
    ),
    defaultMeta: { service: serviceName, environment: env },
    transports: [
        new winston.transports.Console(),
    ],
});

_820‍.d(logger);
